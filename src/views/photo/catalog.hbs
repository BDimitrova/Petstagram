<!-- Starts Main Section -->
<main>
    <div class="container">
        <div class="col-9">
            <!-- Start Pet Photos Post Section -->
            {{#each photo}}
                {{> photo-card}}
            {{else}}
                <article class="not-available-photo">
                    <h1>No photo post yet.</h1>
                </article>
            {{/each}}
        </div>
    </div>
</main>